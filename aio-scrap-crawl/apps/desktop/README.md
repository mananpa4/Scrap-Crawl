# apps/desktop (placeholder)

The desktop app is **planned**, not yet implemented. The intended approach is to
wrap the future `apps/web` front and the `aio` CLI in **Electron or Tauri**,
reusing the same `@aio/core` contract. No code lives here yet; this keeps the
directory out of the build graph. See `TODO.md` → "apps/desktop".
