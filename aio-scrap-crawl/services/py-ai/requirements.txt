# py-ai service — base runtime
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9

# --- Optional engines (install as needed; heavy) ----------------------------
# Markdown / extraction crawler (Apache-2.0)
# crawl4ai>=0.4
# Adaptive / anti-bot stealth scraper with self-healing selectors (BSD-3-Clause)
# HTTP Fetcher needs the [fetchers] extra; stealth/JS also needs `scrapling install`.
# scrapling[fetchers]>=0.4
# Structured extraction via LLM + graphs (MIT)
# scrapegraphai>=2.1
# Agentic browser automation (MIT)
# browser-use>=0.13
# Prompt-injection sanitizer (MIT)
# wipedown>=1.0

# --- Captcha solving (optional) ---------------------------------------------
# 2Captcha commercial solver client (MIT) — needs TWOCAPTCHA_API_KEY
# 2captcha-python>=1.2
# AI-vision solver (self-hosted) would additionally use: openai, google-genai, pillow
